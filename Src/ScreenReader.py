"""DEPRECATED: `ScreenReader.py` has been refactored.

The CLI functionality moved to `screenreader_cli.py` and the live preview
controller moved to `live_clone.py`. This module is kept only for
backwards reference and will raise on import to avoid accidental use.
"""

raise ImportError("ScreenReader.py is deprecated; use Src/screenreader_cli.py or Src/live_clone.py")